import Checkout from "./Checkout"

const CheckoutContainer = () => {
    return (
        <Checkout
            onSubmit={() => {}}
            orderId=""
            isLoading={false}
            />
    )
}

export default CheckoutContainer